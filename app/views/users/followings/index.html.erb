<h1> Following Users </h1>

<table>
  <thead>
    <tr>
      <th> Users </th>
    </tr>
  </thead>
  <tbody>
    <% @followings.each do |f| %>
      <tr>
        <td><%= "#{f.full_name} (@#{f.username})" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
