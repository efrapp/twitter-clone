<div>
  <p>Followers: <%= @followers.size %></p>
  <p>Followings: <%= @followings.size %></p>
</div>

<% if user_signed_in? %>
  <%= link_to 'Create Tweet', new_tweet_path %>
  <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
<% end %>
